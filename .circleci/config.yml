version: 2.1

executors:
  python:
    docker:
      - image: python:3.11

jobs:
  model1-train-and-deploy:
    executor: python
    environment:
      MODEL_NAME: model1
      MODEL_DESC: model1 description text
    steps:
      - checkout
      - run:
          name: install python dependencies
          command: pip install -r requirements.txt --upgrade
      - run:
          name: gather data
          command: python model1/gather_data.py
      - run:
          name: train model
          command: python model1/train_model.py

workflows:
  model1:
    jobs:
      - model1-train-and-deploy