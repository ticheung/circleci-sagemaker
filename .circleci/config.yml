version: 2.1

orbs:
  path-filtering: circleci/path-filtering@0.1.3

parameters:
  deploy-model1:
    type: boolean
    default: false
  deploy-model2:
    type: boolean
    default: false

jobs:
  gather-data:
    docker:
      - image: cimg/python:3.11.2
    steps:
      - checkout
      - run: echo "gather-data"

workflows:
  train-and-deploy:
    jobs:
      - gather-data
